# Postgres connection (from Vercel: add Neon or other Postgres via Marketplace, then POSTGRES_URL is auto-injected)
# Locally: create a DB and paste the connection string here, or use Vercel env pull
POSTGRES_URL=postgres://user:password@host:5432/database?sslmode=require
